import { renderIcon } from '@/utils/index';
import { DashboardOutlined } from '@vicons/antd';

//The front-end routing icon mapping table
export const constantRouterIcon = {
  DashboardOutlined: renderIcon(DashboardOutlined),
};
